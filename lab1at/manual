fact v.0.1 - программа на языке С для вычисления факториала от чисел от 1 до 10.
Copyright (c) Evgeny Parenchenkov <parenche(c)cs.karelia.ru>
Запуск: ./fact
fact.c: main
Основная функция программы, получает на вход целое число, проверяет, что оно находится в диапазоне от 1 до 10, и, если число корректно, вычисляет его факториал.
int argc, char**argv - параметры командной строки (не используются).
Возвращает 0 или 1: 0 означает успех, 1 - ошибку.
fact.h: реализация функции вычисления факториала.
fact - вычисляет факториал от целого числа.
num - начальное число.
ret - результат вычисления факториала.
