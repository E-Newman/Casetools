fact.c: main
Основная функция программы, получает на вход целое число, проверяет, что оно находится в диапазоне от 1 до 10, и, если число корректно, вычисляет его факториал.
int argc, char**argv - параметры командной строки (не используются).
Возвращает 0 или 1: 0 означает успех, 1 - ошибку.
